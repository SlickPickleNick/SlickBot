const {ModalBuilder,ActionRowBuilder,TextInputBuilder,TextInputStyle}=require('discord.js');const {CustomIds}=require('../ui/customIds');
function buildPanelDesignModal(target,name=''){return new ModalBuilder().setCustomId(`${CustomIds.PanelDesignModalPrefix}${target}:${encodeURIComponent(name)}`).setTitle('Edit Panel Design').addComponents(new ActionRowBuilder().addComponents(new TextInputBuilder().setCustomId('title').setLabel('Panel title').setStyle(TextInputStyle.Short).setRequired(false).setMaxLength(256)),new ActionRowBuilder().addComponents(new TextInputBuilder().setCustomId('description').setLabel('Panel description').setStyle(TextInputStyle.Paragraph).setRequired(false).setMaxLength(3000)),new ActionRowBuilder().addComponents(new TextInputBuilder().setCustomId('color').setLabel('Accent color, e.g. #7869ff').setStyle(TextInputStyle.Short).setRequired(false).setMaxLength(7)));}
function parsePanelDesignModalId(id){const s=id.slice(CustomIds.PanelDesignModalPrefix.length);const [target,...rest]=s.split(':');return{target,name:decodeURIComponent(rest.join(':')||'')};}
module.exports={buildPanelDesignModal,parsePanelDesignModalId};
